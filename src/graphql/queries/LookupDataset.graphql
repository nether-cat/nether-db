query lookupDataset($uuid: ID!, $offset: Int!) {
  datasets: Dataset(uuid: $uuid) {
    uuid
    file
    label
    samples
    attributes {
      uuid
      name
    }
    records(first: 100, offset: $offset)
    accessLevel
  }
}
